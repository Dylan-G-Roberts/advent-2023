#include <stdio.h>

int square(int x) {
    return x * x;
}

int sumofsq(int x, int y) {
    return square(x) + square(y);
}

int

int abs(int x)
{
    if (x > 0)
        return x;
    else if (x < 0)
        return -x;
    else
        return 0;
}

int main() {
    char c;

    printf(" %d\n", abs(-3));
}